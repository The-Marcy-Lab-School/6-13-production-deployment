# Variable used to "sign" each cookie to avoid tampering. This will be encrypted and should never be shared.
# Try the command `openssl rand-hex 32` to generate a secret
SESSION_SECRET=""

# Variables used for connecting to your local Postgres server
PGHOST=localhost
PGPORT=5432
PGDATABASE=users_db
PGUSER=""
PGPASSWORD=""

# When testing a production database, set this to your hosted database URL instead of the vars above
PG_CONNECTION_STRING=""


